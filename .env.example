# ═══════════════════════════════════════════════════════════
# Deep Research Agent - Environment Configuration
# ═══════════════════════════════════════════════════════════

# ── LLM: LiteLLM (preferred) or direct provider keys ──
# If LITELLM_API_KEY is set, all models are called via the LiteLLM proxy; no need for provider keys here.
LITELLM_API_KEY=sk-...
LITELLM_API_BASE=http://localhost:4000

# Direct provider keys (used only when LITELLM_API_KEY is not set)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# ── Fast-tier models (optional; used for routine extraction / debate; default: sonnet, gpt-4.1-mini, gemini-2.0-flash) ──
# CLAUDE_FAST_MODEL=claude-sonnet-4-20250514
# OPENAI_FAST_MODEL=gpt-4.1-mini
# GEMINI_FAST_MODEL=gemini-2.0-flash

# ── Search APIs ──
TAVILY_API_KEY=tvly-...
BRAVE_SEARCH_API_KEY=BSA...
# SEC EDGAR User-Agent compliance (required for sec.gov requests)
SEC_CONTACT_EMAIL=hvsolanki27@gmail.com
# Optional: use crawl4ai as extra fetch tier for regulatory domains (requires: pip install -e ".[fetch-crawl4ai]")
USE_CRAWL4AI_FETCH=false

# ── Neo4j (use bolt://localhost:7687 with docker-compose) ──
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password

# ── LangSmith (optional; set to true only if you have a valid key) ──
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=lsv2_pt_...
LANGCHAIN_PROJECT=deep-research-agent

# ── Agent Config ──
MAX_SEARCH_ITERATIONS=8
MAX_SEARCH_RESULTS_PER_QUERY=10
CONFIDENCE_THRESHOLD=0.6
LOG_LEVEL=INFO

# ── Cost budget (USD per investigation; 0 = no limit) ──
COST_BUDGET_USD=5.0
